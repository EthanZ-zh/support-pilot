# 写请求幂等键

## 请求头与作用域

创建类请求通过 `Idempotency-Key` 提供幂等语义。键在租户和具体操作组成的 scope 内唯一，不同租户可以使用相同文本键而互不影响。

## 相同与不同载荷

相同键和相同规范化载荷返回第一次成功创建的资源。相同键但载荷 hash 不同返回 HTTP 409，服务不会覆盖第一次结果。

## 保存期限与并发

ExampleAPI v2 的幂等结果默认保存二十四小时。并发请求由数据库唯一约束和事务协调；不能依赖应用进程内锁作为最终防线。

