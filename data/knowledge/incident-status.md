# 服务状态与事故判断

## 关联条件

事故关联必须同时考虑服务组件、区域和客户报告时间。只有三项与事故影响窗口匹配，才能表述为已确认关联；只匹配错误码不足以证明属于平台事故。

## 没有匹配事故

“未发现匹配事故”不等于“服务正常”。这时应继续收集脱敏 request_id、区域、时间范围和错误码，查询组件状态；证据仍不足时创建工单。

## 客户可见信息

回复只能包含 public incident code、公开状态、影响范围和 customer_message。internal_notes 仅内部支持角色可见，不能进入客户回答或公开引用。

