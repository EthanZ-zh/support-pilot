# 游标分页

## 请求参数

列表接口使用 `limit` 和 `cursor`。limit 默认 50，最大 200。cursor 是不透明字符串，客户端不能解析、修改或自行生成。

## 下一页

响应中的 `next_cursor` 非空时才存在下一页。下一次请求必须保持相同过滤和排序参数，否则 cursor 会失效并返回 400。

## 一致性与重复

分页期间数据可能变化，客户端应按资源 id 去重。需要稳定快照的批量读取应使用导出任务，而不是无限翻页。

