# Webhook 投递与重试

## 成功确认

接收端在二十秒内返回任意 2xx 即视为成功。重定向、4xx、5xx 和网络超时都不会被视为成功投递。

## 重试计划

失败事件使用指数退避，在最多二十四小时内有限重试。每次投递保持相同 event_id，但 delivery_id 不同；消费者必须按 event_id 去重。

## 排查投递失败

先检查 endpoint 是否公开可达、TLS 证书是否有效、响应是否超时，再按 delivery_id 查询投递记录。不要通过关闭签名验证来解决投递失败。

