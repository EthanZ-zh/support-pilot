# SupportPilot 项目协作指令

## 1. 用户与项目目标

- 用户是 2027 届软件工程本科生，目标岗位为大模型应用开发、AI Agent 开发和 Agent 全栈开发实习。
- SupportPilot 是面向企业 SaaS 的技术支持与智能工单 Agent，目标不是制作聊天 Demo，而是完成一套可运行、可评测、可解释、可演示、可写入简历的业务闭环。
- 项目必须帮助用户真正掌握实现。除了交付代码，还要让用户能够画出架构、讲清请求链路、解释技术选型、定位问题并提出改进方案。
- 优先保证业务合理性、技术深度和完成度，不为了堆技术名词引入没有实际价值的组件。

## 2. 产品边界

SupportPilot 的核心闭环是：

`用户问题 → 意图与风险识别 → 知识检索/业务工具 → 解决方案 → 置信度门控 → 自动回复或创建工单 → 人工接管 → 反馈回流`

首要用户：

- 使用企业 SaaS 产品的客户；
- 一线技术支持人员；
- 负责知识、工单和质量管理的管理员。

核心数据源：

- 非结构化数据：产品文档、API 文档、版本说明、FAQ、Known Issues、Runbook、历史工单解决方案；
- 结构化数据：租户、套餐、功能权限、服务状态、事故、工单、反馈与审计记录。

动作风险分级：

- 查询知识、套餐、权限、服务状态和已知事故属于只读动作，可自动执行；
- 创建工单或生成回复草稿属于低风险写入，必须具备幂等和审计；
- 修改权限、退款、执行运维命令等高风险动作不得自动执行，必须人工批准；第一版可以只提供模拟或草稿能力。

不做以下伪装：

- 不把合成数据写成真实企业生产数据；
- 不虚构真实公司客户、生产流量、线上收益或实习经历；
- 不把离线小样本指标表述为生产准确率；
- 不声称尚未实现或尚未验证的能力。

## 3. 技术与架构原则

- 默认采用模块化单体和清晰分层，先完成纵向业务闭环，再考虑拆分服务。
- 后端优先使用 Python 3.11+、FastAPI、Pydantic、SQLAlchemy 2、Alembic、PostgreSQL/pgvector；前端优先使用 React、TypeScript 和 Vite。
- Agent 编排优先使用 LangGraph；模型、Embedding 和 Reranker 必须通过 Provider 接口隔离，禁止业务代码依赖单一厂商。
- RAG 逐步实现关键词与向量双路召回、RRF 融合、Rerank、元数据过滤、引用溯源和 Answerability Gate。
- 业务数字、权限判断、状态迁移和合规规则使用确定性代码或数据库，不让 LLM 猜测。
- Agent 状态、工具输入输出和模型结构化输出必须有明确 Schema；所有外部输入都要验证。
- 工作流必须支持超时、有限重试、失败降级和可解释 Trace；禁止无限 Agent 循环。
- 写操作必须考虑事务、状态机、幂等键、唯一约束和并发冲突。
- 默认最小权限：只读优先，高风险工具必须显式授权并保留审计。
- API Key 和 Token 只能放在被 Git 忽略的环境文件中；日志、测试快照和文档不得出现密钥或完整敏感数据。
- 不为了简历使用微服务、Kubernetes、消息队列或 GraphRAG；只有业务或验证需求成立时才引入，并记录取舍。

## 4. 分阶段交付

按可验证的阶段推进，每个阶段都必须形成可运行增量：

1. 产品定义：用户、痛点、用例、非目标、风险边界、数据字典、验收标准；
2. 确定性基线：数据库、租户/权限/事故/工单模型、业务工具和不依赖 LLM 的主流程；
3. RAG：数据摄取、混合召回、重排、引用、门控与检索评测；
4. Agent：意图路由、工具调用、诊断规划、工单创建、失败降级和持久化状态；
5. 安全闭环：JWT/RBAC、人工接管、状态机、幂等、Prompt Injection 防护与审计；
6. 全栈体验：React 控制台、SSE 进度、证据展示、工单与人工处理页面；
7. 工程质量：OpenTelemetry、测试矩阵、CI、Docker Compose、迁移检查和演示脚本；
8. 求职交付：README、架构图、演示脚本、评测报告、问题复盘、简历描述与面试问答。

不得在只有目录或空壳接口时宣布某阶段完成。

## 5. 评测与测试要求

建立分层验证，不能只测试最终回答：

- 单元测试：领域规则、状态机、权限、幂等、解析、RRF 和指标计算；
- 集成测试：PostgreSQL/pgvector、迁移、检索、Checkpoint、API 和工具调用；
- Agent 场景测试：正常、缺少参数、无答案、工具失败、重复请求、并发、越权和 Prompt Injection；
- 前端测试：关键组件、SSE 状态更新和人工接管流程；
- 端到端测试：用户提问到自动回复或创建工单的完整闭环。

至少跟踪以下评测指标：

- 检索 Recall@K、MRR、nDCG；
- 引用正确率、忠实度和 Answerability；
- 意图分类与人工升级的 Precision/Recall/F1；
- 工具参数准确率、工具成功率和工单字段完整率；
- 高风险误执行率、重复副作用数；
- 端到端延迟、模型调用次数、Token 和估算成本。

评测集要区分合成数据、人工标注数据和真实公开数据；报告样本量、Provider、模型、时间和失败项，不隐藏缺失值或失败样本。

## 6. 决策与用户确认

- 对现有模式内的低风险实现细节，直接作出合理决定并说明依据，不反复询问。
- 当选择会明显改变产品方向、开发周期、模型费用、数据合法性、外部服务、部署成本或高风险写操作时，先给出 2 至 3 个可比较方案，让用户决定。
- 技术选型必须回答：解决什么问题、为什么现在需要、替代方案是什么、代价是什么、如何验证。
- 重要架构决策写入 `docs/adr/`；业务范围和阶段验收写入 `docs/product/`，避免只存在聊天记录中。
- 需求变化时先更新产品文档和验收标准，再修改实现。

## 7. 教学与面试准备

- 默认使用中文解释，代码、类型和接口名称保留英文。
- 每个阶段开始前说明业务目标、请求链路、核心文件和本阶段验收标准。
- 每个阶段结束后提供：架构变化、关键代码入口、一次请求的完整链路、遇到的问题与修复、技术取舍、用户需要掌握的知识点和高频面试追问。
- 解释代码时优先沿真实请求链路展开，不按目录机械罗列文件。
- 对重要实现给出 30 秒、3 分钟和深入追问三种讲法。
- 主动指出项目缺陷和限制，给出可落地改进方案，不将项目描述成完美生产系统。
- 简历描述只能使用已实现且已验证的数据；保留可复现命令、报告或测试作为证据。

## 8. 开发工作流

每次变更遵循：

1. 读取相关 `AGENTS.md`、产品文档、架构决策和现有代码；
2. 明确目标、影响范围和可验证的完成标准；
3. 查看 Git 状态，保护用户已有改动；
4. 优先实现最小纵向闭环，避免大批空壳文件；
5. 编写或更新与风险相称的测试；
6. 运行相关测试、lint、类型检查、迁移检查或构建；
7. 检查 diff，删除调试代码，确认没有无关修改和敏感信息；
8. 更新必要的 README、架构图、ADR、评测报告和学习说明；
9. 汇报已完成内容、验证证据、尚存风险和下一步。

若检查失败：

- 本次修改引起的失败必须先修复再汇报；
- 既有或环境问题要给出证据、影响和复现命令；
- 未运行真实数据库、模型或浏览器测试时，不得声称对应能力已验证。

## 9. 完成定义

功能只有同时满足以下条件才算完成：

- 行为符合产品用例和风险边界；
- 代码、迁移、配置和文档一致；
- 关键路径有自动化测试或明确的人工验证记录；
- 错误、超时、空结果和越权路径得到处理；
- 运行方式可复现，仓库不包含密钥和本地生成物；
- 指标和项目描述有证据支持；
- 用户能够说清该功能为什么存在、如何运行、有哪些限制。

